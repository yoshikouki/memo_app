.list-group.list-group-flush.my-5
  - if @memos
    - @memos.each do |memo|
      %a.list-group-item{ href: "/#{memo.id}", role: "button" }
        = memo.title
  - else
    %a.list-group-item{ href: "/new", role: "button" }
      New memo
