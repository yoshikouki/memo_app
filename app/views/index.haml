.list-group.list-group-flush.my-5
  - if @memos.nil?
    %a.list-group-item{ href: "/new", role: "button" }
      New memo
  - else
    - @memos.each do |content|
      %a.list-group-item{ href: "/#{content.title}", role: "button" }
        = content.title
